1️⃣ List (Ordered, Changeable, Duplicates Allowed)
A collection that is ordered and mutable (can change).
Allows duplicate values.
Uses square brackets [ ].

✅ Example:
fruits = ["apple", "banana", "cherry"]
fruits.append("orange")  # Add item
print(fruits)  # ['apple', 'banana', 'cherry', 'orange']


2️⃣ Tuple (Ordered, Unchangeable, Duplicates Allowed)
A collection that is ordered but immutable (cannot change).
Allows duplicate values.
Uses parentheses ( ).

✅ Example:
colors = ("red", "blue", "green")
print(colors[1])  # Output: blue


3️⃣ Dictionary (Unordered, Key-Value Pairs, Unique Keys)
A collection of key-value pairs.
Keys must be unique, values can be changed.
Uses curly braces { }.

✅ Example:
person = {"name": "John", "age": 25}
print(person["name"])  # Output: John


🔹 Quick Comparison
Feature	               List         Tuple	       Dictionary
Ordered	              ✅ Yes       ✅ Yes	     ❌ No (Python 3.7+ maintains order)
Changeable	        ✅ Yes	   ❌ No	          ✅ Yes
Duplicates Allowed	  ✅ Yes       ✅ Yes	     ❌ No (Keys must be unique)
Uses	                 [ ]	       ( )           { }
These are important data structures in Python for storing and managing data! 







